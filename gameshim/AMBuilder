# vim: set ts=2 sw=2 tw=99 noet ft=python:

for target in MMS.sdk_targets:
	sdk = target.sdk
	name = 'stripper.' + sdk['extension']

	binary = MMS.HL2Library(builder, target.cxx, name, sdk)

	binary.sources = [
		'stripper_mm.cpp',
	]

	nodes = builder.Add(binary)
	MMS.binaries += [nodes]
